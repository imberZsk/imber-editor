{
  "extends": ["next/core-web-vitals", "prettier"],
  "rules": {
    "no-var": "error",
    "no-unused-vars": [
      "error",
      // 结合TS的"noUnusedLocals": true,
      { "varsIgnorePattern": ".*", "args": "none" }
    ]
  }
}
